import os 

for csv_file in os.listdir("activities"):
    if csv_file.endswith(".csv"):
        continue 




data = load_data(f"activities/{csv_file}")

sorted_data = sort_data(data)

save_plot(sorted_data, output_path)

print(f"Grafik erstellt und gespeichert: {output_path}")
